describe('ng.cork.throttling', function () {
    'use strict';

    beforeEach(module('ng.cork.throttling'));

    describe('corkThrottling', function () {

        describe('default state', function () {

            it('should initialise with known defaults.', inject(function (corkThrottling)  {

                //expect(corkThrottling.divisor).toEqual(2);
                //expect(corkThrottling.rounding).toEqual(false);
            }));

        });
    });
});
